# Include inside the existing citybank.abmgroup.tech HTTPS server block.
client_max_body_size 100m;
client_body_timeout 180s;
proxy_connect_timeout 30s;
proxy_read_timeout 300s;
proxy_send_timeout 300s;
# Remove/raise any smaller overrides in the existing location / or location /api blocks.
